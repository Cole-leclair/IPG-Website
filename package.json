{
  "name": "ipg-website",
  "private": true,
  "description": "ipg.team static site + Netlify Functions backend (portal API). No build step for the frontend; this file exists only to declare function dependencies for Netlify's esbuild bundler.",
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
